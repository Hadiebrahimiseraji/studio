from django.urls import path

urlpatterns = [
    # Define cart-related URLs here
    # path('cart/', views.CartDetail.as_view(), name='cart-detail'),
    # path('cart/add/', views.AddToCart.as_view(), name='add-to-cart'),
    # path('cart/remove/<int:item_id>/', views.RemoveFromCart.as_view(), name='remove-from-cart'),
]